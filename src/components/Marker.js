import React from 'react';

/** Dumb component to place image as marker
 * in GMaps component.
 */
const Marker = () => (
  <div>
    <img alt="Marker" src="images/marker.svg" style={{ height: '40px' }} />
  </div>
);

export default Marker;
